# 雨痕数据恢复 V1.15.3

- 修复 PhotoRec 发布运行环境：补齐官方 `63/cygwin` 文件，并在子进程启动时显式设置 `TERMINFO`。
- 修复 4K 逻辑扇区 NTFS 实盘验收入口，G 盘误删除恢复 13/13 SHA-256 通过。
- 验收报告不再把 PhotoRec 的 `report.xml`、`photorec.log` 和 `photorec.ses` 统计为额外恢复文件。
- E/exFAT 与 G/NTFS 误删除实盘均达到 13/13 原名、原路径、完整内容恢复。
- 快速格式化实盘如实记录：E 内容扫描完整找回 7/13，G 完整找回 2/13；元数据被覆盖后不承诺原名。
- Release 编译 0 警告、0 错误，40 项核心回归及 WPF 自动化全部通过。

使用前请阅读同目录的《雨痕数据恢复-使用教程.md》。源介质始终只读，恢复输出必须选择另一块物理磁盘。
